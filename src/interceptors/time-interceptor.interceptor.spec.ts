import { TimeInterceptorInterceptor } from './time-interceptor';

describe('TimeInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new TimeInterceptorInterceptor()).toBeDefined();
  });
});
